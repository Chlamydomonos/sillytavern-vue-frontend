{
    "name": "sillytavern-vue-frontend",
    "scripts": {
        "build": "cd packages/rollup-plugin && pnpm build && cd ../frontend-event-emitter && pnpm build && cd ../extension && pnpm build"
    }
}
