export enum ExtensionPromptPosition {
    AFTER_STORY,
    IN_CHAT,
}

export enum ExtensionPromptRole {
    SYSTEM,
    USER,
    ASSISTANT,
}

export enum EntryPosition {
    BEFORE_CHAR_DEF,
    AFTER_CHAR_DEF,
    BEFORE_AUTHOR_NOTE,
    AFTER_AUTHOR_NOTE,
    IN_DEPTH,
    BEFORE_EXAMPLE_MES,
    AFTER_EXAMPLE_MES,
}
